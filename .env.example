# Copy to .env.local and paste the key from your Paystack dashboard.
# Without it the shop sends orders to WhatsApp instead of opening checkout.
REACT_APP_PAYSTACK_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
